import { Injectable } from "@angular/core";
import { Observable } from "rxjs";
import io from "socket.io-client";
import { socketUrl } from "../components/constants/socketUrl";

@Injectable({
  providedIn: "root",
})
export class SocketService {
  
}
