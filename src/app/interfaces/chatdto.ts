export interface ChatDto {

message: string,
doctorId: string | null,
doctorName: string | null,
userId: string | null,
userName: string ,
conversationId: string | null,
image: any,
lastmsg: string ,
}